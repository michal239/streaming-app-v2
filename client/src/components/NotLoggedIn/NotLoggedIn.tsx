import React from 'react';
import './NotLoggedIn.scss';

const NotLoggedIn: React.FC = () => {
  return <div className="not-logged-in">You must be logged in to view this page</div>;
};

export default NotLoggedIn;
